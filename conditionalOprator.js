let x=2;
    let message= (x<3) ? "Hi babey" : (x<18) ? " hi young man": (x<100) ?'Greetings!': "what an unsual age";
    console.log(message);

    let name =prompt("please enter the old name of JavaScript","ECMAScript");
    if(name=="ECMAScript")
    {
        alert("Welcome to JavaScript");
    }
    else
    {
        alert("you are not welcome");
    }
